# scrapy-web
This repo includes a holiday directory which contains the .ics files I scraped from web, a country_ISO .csv file for later query, and a python script file.

This python script includes method of 
1. downloading target files from web,
2. upload to aws s3 database
3. create a table in dynamodb for query.
